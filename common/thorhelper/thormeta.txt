Questions arising out of walking through the sketched out implementation:

How do partitions fit in?
When are locations resovled?  Early reduces the amount of data transferred.  Later allows better fallback
Review superfile ordering

What information is signed?  Is it just the logical filename, and nothing else, or also some essential characteristics?
How do we reuse buffering classes cleanly?

Implementing FETCH and parition reading required basic interface to request reading for a range of the file.  Fits in well with the range support of S3/azure

What is the relationship between copies and duplication on other clusters?
Replacing :: with __scope__ in temporary files should be done much later on.

Where verify the format crcs?
